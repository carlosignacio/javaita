import javax.swing.JFrame;
import javax.swing.JOptionPane;

public class Principal {

	public static void main(String[] args) {
		
		int menuPrincipal = Integer.parseInt(JOptionPane.showInputDialog(null, MenuPrincipal(), "Login do Sistema", JOptionPane.DEFAULT_OPTION));
		switch (menuPrincipal) {
		case 1:
			//cadastrarUsuario();
			break;
		case 2:
			//consultarUsuario();
			break;
		case 3:
			JanelaLogin();;
			break;
		default:
			JOptionPane.showMessageDialog(null, "Opção do menu inválida!");
			break;
		}
	}	
	
	public static void JanelaLogin() {
	
	JFrame frame = new JFrame();
	
	frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
	frame.setSize(300, 300);
	frame.setVisible(true);

	
	}
	public static String MenuPrincipal() {
		String menu = " ";
		menu += "Digite 3 para Logar no Sistema";
		return menu;
	}
}
